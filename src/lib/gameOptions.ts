export type GameOptions = {
    sound: boolean
    speedupStart: boolean
    robberAnimation: boolean
}

export const defaultGameOptions: GameOptions = {
    sound: true,
    speedupStart: false,
    robberAnimation: true,
}
